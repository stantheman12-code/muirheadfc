import React from "react";

{/* Funtion component for the Header section of the webapp, which also contains the Navigation child component */}

export default function Header() {
    return (
        <div className="HeaderSection">
            <Navigation />
        </div>
    );
}